syntax on
set number

"For setting tab width and changing tabs to spaces
set tabstop=2
set shiftwidth=2
set expandtab

"For indenting
set smartindent
set autoindent

"For autocompletion
set wildmode=list:longest

set foldenable
set ruler

"For highlighting search results
set hlsearch

"For enabling incremental search, aka search-as-you-type
set incsearch

"For toggling paste option to auto-indent pasted text
set pastetoggle=<F6>

"For displaying the file name at the bottom
set modeline
set ls=2

"For adjusting colors for dark backgrounds
set background=dark
